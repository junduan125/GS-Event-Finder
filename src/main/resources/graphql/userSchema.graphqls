extend type Mutation {
	addUserCharacter(characterType: Int!, level: Int): GSUser
	editUserCharacter(id: ID, characterType: Int): GSUser
	removeUserCharacter(id: ID): GSUser
}

type GSUser {
	username: String!
}

type UserCharacter {
	level: Int
}